;GET ALL OF THE TABLE NAMES FROM DM_COLUMN_DOCS THAT DON'T HAVE A NUMBER IN THEM AND CREATE A DYNAMIC QUERY
;THIS GETS ALL TABLE NAMES THAT HAVE EKS IN THE TITLE
select DISTINCT "SELECT * FROM ", DM.table_name, " WITH MAXREC = 50 GO; "
from dm_columns_doc dm
plan DM
where dm.table_name = "*EKS*"
AND NOTOPERATOR(DM.table_name,"REGEXPLIKE","[0-9]")
